import { ViewStyle } from 'react-native'

interface RemoveButtonStylesType {
  left: ViewStyle
  right: ViewStyle
}
const RemoveButtonStyles: RemoveButtonStylesType = {
  left: { marginLeft: 30 },
  right: { marginRight: 30 }
}

export default RemoveButtonStyles
